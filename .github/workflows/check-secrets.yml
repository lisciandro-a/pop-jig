name: Check for Secrets

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ lisciandro-a-patch-1 ]
    
    
jobs:
  check-secrets:
    runs-on: [ ubuntu-latest ]
    uses: docker://alisciandro/whispers
    
    steps:
    
    - name: Did I even get this far
      run: echo "Yes"
